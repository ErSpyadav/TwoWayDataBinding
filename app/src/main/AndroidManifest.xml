<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.expresso.twowaydatabing">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity-->
<!--            android:name=".bindingadapter.BindingAdapterActivity"-->
<!--            android:label="@string/title_activity_binding_adapter"-->
<!--            android:theme="@style/AppTheme.NoActionBar"></activity>-->
        <!-- <activity -->
        <!-- android:name=".ListernerBindActivity" -->
        <!-- android:label="@string/title_activity_listerner_bind" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
        <!-- <activity -->
        <!-- android:name=".twowaybinding.MainActivity" -->
        <!-- android:label="@string/title_activity_one_way_binding" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".bindingadapter.BindingAdapterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>